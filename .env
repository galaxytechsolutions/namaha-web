# Razorpay key - MUST match the backend's Razorpay key_id exactly.
# "The id provided does not exist" = frontend key != backend key. Restart dev server after changes.
REACT_APP_RAZORPAY_KEY_ID=rzp_live_SMgsnajeiKNOHB
# API base URL – used when running on localhost (e.g. npm start)
REACT_APP_API_URL_LOCAL=http://localhost:8080/api
# Production API (used when not on localhost)
REACT_APP_API_URL=https://api.shriaaum.com/api  


